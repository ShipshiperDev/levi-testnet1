# Dependencies
node_modules/
.pnp
.pnp.js

# Build artifacts
.next/
out/
build/
dist/
artifacts/
cache/
typechain/
typechain-types/

# Environment variables (EXCLUDING SAFE ONES SENSITIVE KEY CASE)
# We will manually add web/.env.local because it contains public contract data,
# but we will ignore the one with the PRIVATE_KEY.
contracts/.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# System
.DS_Store
Thumbs.db

# Temporary folders
contracts_temp/
